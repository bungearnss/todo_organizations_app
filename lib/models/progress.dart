import 'package:flutter/material.dart';

class Progress {
  double percent;
  String title;
  Color color;

  Progress({
    required this.percent,
    required this.title,
    required this.color,
  });
}
